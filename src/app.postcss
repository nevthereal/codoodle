@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

input {
	@apply input p-2;
}

textarea {
	@apply textarea p-2;
}

p.post-content > p > a {
	@apply anchor;
}

p.post-content > p > code {
	@apply code;
}
